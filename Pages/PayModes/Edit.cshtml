@page
@model SupermarketWEB.Pages.PayModes.EditModel
@{
	ViewData["Title"] = "Edit Pay Mode";
}

<div class="container mt-4">
	<h2 class="mb-3">Edit Pay Mode</h2>

	<div class="card">
		<div class="card-body">
			<form method="post">
				<input type="hidden" asp-for="PayMode.Id" />
				<div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

				<div class="mb-3">
					<label asp-for="PayMode.Name" class="form-label"></label>
					<input asp-for="PayMode.Name" class="form-control" />
					<span asp-validation-for="PayMode.Name" class="text-danger"></span>
				</div>

				<div class="mb-3">
					<label asp-for="PayMode.Description" class="form-label"></label>
					<input asp-for="PayMode.Description" class="form-control" />
					<span asp-validation-for="PayMode.Description" class="text-danger"></span>
				</div>

				<div class="d-flex justify-content-between">
					<button type="submit" class="btn btn-success">Save</button>
					<a asp-page="./Index" class="btn btn-outline-primary">Back</a>
				</div>
			</form>
		</div>
	</div>
</div>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}